/*11.Find circumference of square formula : C = 4 * a*/
#include <stdio.h>
int main() {
	int circumference,length;
    printf("Enter the length");
    scanf("%d", &length);
    circumference = 11*length;
    printf("The circumference of the square is: %d", circumference);
}

