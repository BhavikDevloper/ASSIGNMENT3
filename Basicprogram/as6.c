/*6.Find area of Triangle Formula : A = 1/2 × b × h*/
#include <stdio.h>
int main() {
    float base, height, area;
    printf("Enter the bas");
    scanf("%f", &base);
    printf("Enter the height");
    scanf("%f", &height);
    area = 0.5 * base * height;
    printf("%f", area);
}

